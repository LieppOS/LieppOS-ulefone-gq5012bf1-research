# Build verification

## Reconstruction

- Target: `//lieppos/custom-ldo-wl2868-recon/recon:custom_ldo_wl2868_recon`
- Kernel: `//common:kernel_aarch64`
- Final `BUILD_RC=0`
- unresolved/module modpost symbols: 0
- stock kernel import set: 27; reconstructed llvm-nm set: 26 plus the MODVERSION-only `module_layout` record
- export CRCs: 2/2 exact, generated by Kbuild/genksyms
- no CRC patching and no hand-written Module.symvers

The final log is `workspace/phase4-custom-ldo-wl2868/recon-build.log`. Kleaf reports two OpenSSL/sign-file diagnostics because `common/certs/signing_key.pem` is absent in the local workspace; no compiler warning, modpost warning, or unresolved relocation was reported.

## RED donor

The pristine Sony RED target fails intentionally at the legacy probe callback type:

`int (*)(struct i2c_client *, const struct i2c_device_id *)` versus donor `int (struct i2c_client *)`.

It is retained as `workspace/phase4-custom-ldo-wl2868/sony-donor-red-build-final.log` and remains structural-only.
