load("//build/kernel/kleaf:kernel.bzl", "kernel_module")

package(default_visibility = ["//visibility:public"])

kernel_module(
    name = "fingerprint_recon",
    srcs = ["Makefile", "fingerprint.c"],
    outs = ["fingerprint.ko"],
    kernel_build = "//common:kernel_aarch64",
)
