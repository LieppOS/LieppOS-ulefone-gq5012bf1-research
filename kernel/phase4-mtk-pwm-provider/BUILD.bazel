load("//build/kernel/kleaf:kernel.bzl", "kernel_module")

package(default_visibility = ["//visibility:public"])

kernel_module(
    name = "mtk_pwm_provider",
    srcs = glob(["**/*"], exclude = ["BUILD.bazel"]),
    outs = ["mtk-pwm.ko"],
    kernel_build = "//common:kernel_aarch64",
)
